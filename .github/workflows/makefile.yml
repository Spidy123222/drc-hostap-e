name: Makefile CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: packages
      run: apt install docbook-to-man

    - name: build
      run: ./build_release 2.6

    - name: Run check
      run: make check

    - name: Run distcheck
      run: make distcheck
